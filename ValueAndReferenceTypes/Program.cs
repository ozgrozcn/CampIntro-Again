using System;

namespace ValueAndReferenceTypes
{
    class Program
    {
        static void Main(string[] args)
        {
            // Sayısal veri tipleri: int, decimal, byte, float, double, bool değer tiptir.
            // Array, class, interface referans tiptir.
        }
    }
}